-- Beispiel Quests von deinem Server - als Referenz
-- Diese zeigen die Quest-Syntax die auf deinem Server funktioniert

-- Beispiel aus guild_safebox.txt:
quest guild_safebox begin
	state start begin
		when 11000.chat."Gildenlager kaufen" with pc.has_guild() and pc.is_guild_master() and not guild.has_safebox() begin
			say_title(mob_name(npc.get_race())..":")
			say("Du möchtest ein Gildenlager kaufen?")
			-- ... dein Code ...
		end
	end
end

-- Beispiel aus item_convert.txt:
quest item_drop_convert begin 
	state start begin
		when 71649.receive begin
			local item_to_change = 71649
			local count_needed = 250
			-- ... dein Code ...
		end
	end
end

-- Wichtige Funktionen die auf deinem Server funktionieren:
-- pc.get_id()
-- pc.say()
-- pc.give_item2()
-- pc.remove_item()
-- pc.getqf() / pc.setqf()
-- select()
-- npc.get_race()
-- mob_name()